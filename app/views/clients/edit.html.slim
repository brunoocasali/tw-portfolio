h2
 = t('edit', m: t('.title'))
 = link_to t('back'), clients_path, class: 'btn btn-default btn-sm pull-right'
 = link_to t('show_btn'), client_path(@client), class: 'btn btn-primary btn-sm pull-right'

== render 'form'
